class Node{
    int element;
    Node next;

    Node(int element){
        this.element = element;
    }

    Node(int element, Node next){
        this.element = element;
        this.next = next;
    }
}